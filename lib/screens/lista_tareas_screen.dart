import 'package:flutter/material.dart';

class ListaTareasScreen extends StatelessWidget {
  const ListaTareasScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Matrix Cargando...'),
      ),
    );
  }
}